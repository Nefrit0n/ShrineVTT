# ShrineVTT environment configuration
# Copy this file to .env and adjust values as needed.

# HTTP port exposed by the backend server.
PORT=8080

# Allowed origins for CORS requests. Separate multiple origins with commas.
CORS_ORIGIN=http://localhost:8080

# Secret used to sign and verify JSON Web Tokens. Change this in production.
JWT_SECRET=change-me

# Absolute or relative path to the SQLite database file used by the backend.
SQLITE_DB_PATH=./data/shrine.db

# Optional log level for the backend logger (trace, debug, info, warn, error, fatal).
LOG_LEVEL=info
